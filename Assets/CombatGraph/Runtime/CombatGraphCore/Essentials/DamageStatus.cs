namespace CombatGraph
{ 
    public enum DamageStatus
    {
        Dodged, //avoid
        Landed, // normal
        Caped, // caped
        Blocked, // invincible
        Critical, // critical
        Dead, // dead
    }
}

